name: Container cleanup

on:
  schedule:
    - cron: "17 3 * * 0"
  workflow_dispatch:
    inputs:
      execute:
        description: Delete old GHCR versions (manual runs are dry-run by default)
        required: true
        default: false
        type: boolean

permissions:
  contents: read
  packages: write

jobs:
  cleanup:
    uses: devsecopslonghn/.github/.github/workflows/container-cleanup.yml@master
    with:
      package_name: lol-analysis-frontend
      chart_values_url: https://raw.githubusercontent.com/${{ vars.GITOPS_LOL_ANALYSIS_REPOSITORY }}/${{ vars.GITOPS_BRANCH }}/values.yaml
      execute: ${{ github.event_name == 'schedule' || inputs.execute }}
    secrets: inherit
